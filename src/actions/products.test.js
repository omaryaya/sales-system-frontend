import * as TYPES from './types';
import { createProduct } from './products';

const product = {
    name: "",
    categoryId: 0,
    sku: "",
    price: "",
    order: {}
}

it('Empty Create Product', () => {
    /* createProduct(product);
    expect(createProduct)
        // .toHaveBeenCalledWith({ ...product })
        .toHaveBeenCalled() */
})