import React from 'react';
import PropTypes from 'prop-types';

const Customer = props => {
    return (
        <div>
            
        </div>
    );
};

Customer.propTypes = {
    
};

export default Customer;